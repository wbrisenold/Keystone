# Validation

Local checks cover source lifecycle invariants, C++ compilation, model tests, and ZIP integrity. GitHub Actions performs the universal macOS build, export checks, dependency inspection, bundle validation, and code signing. Resolve runtime loading still requires testing in Resolve itself.
